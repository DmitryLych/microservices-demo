spring:
  cloud:
    config:
      uri: http://localhost:8001
server:
  port: ${PORT:${SERVER_PORT:0}}

---
spring:
  profiles: subject
  application:
    name: subject

---
spring:
  profiles: verb
  application:
    name: verb

---
spring:
  profiles: article
  application:
    name: article

---
spring:
  profiles: noun
  application:
    name: noun
---
spring:
  profiles: cold-noun
  application:
    name: noun
